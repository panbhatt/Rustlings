

fn main() {
    //let x: usize ; // This is an error, Rust prohibits you from using a variables unless it is being touched. so we have to explicitly initialize it. 
    let x: i32 = 999 ; 

    println!("{}", x);  
}